import { computed } from 'vue';
export function smartComputed(getter, comparator = (newValue, oldValue) => JSON.stringify(newValue) === JSON.stringify(oldValue)) {
    return computed((oldValue) => {
        const newValue = getter();
        return oldValue === undefined || !comparator(newValue, oldValue)
            ? newValue
            : oldValue;
    });
}
